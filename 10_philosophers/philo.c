#include "philo.h"

int main(int argc, char *argv[])
{
    if (argc < 4 || argc > 6)
    {
        ft_putstr("Error: arguments\n");
        return (1);
    }
    if (argv[0])
    {
        // Continue
    }
    return (0);
}