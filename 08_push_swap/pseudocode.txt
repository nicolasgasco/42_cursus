1. Find smallest number
    First time
        If on top or left -> Shift up
        If in the middle -> check B?
        If at bottom or right -> Shift down
    B is empty
        Search in A
    B is not empty
        Search in B

        IF IN A -> repeat
        IF IN B -> 
            If on top -> move back to A
            Else -> Shift up or down depending on what better
2. Move smallest up and push preceding numbers
    If numbers in B >= 2 -> check if moving with A or not
        if A[0] > A[1] -> Move
        Else -> Stay still
3. Smallest is on top
    Shift up
    Check if B shifs or not