#include "push_swap.h"

int	main(int argc, char *argv[])
{
	int		i;
	char	*nums;
	char 	**nums_arr;
	int		*result;

	ft_create_linked_list(argc, argv);
	// i = 1;
	// printf("%s\n", nums);
	// i = 0;
	// nums_arr = ft_split(nums, ',');
	// result = (int *)malloc(sizeof(int) * (argc - 1));
	// while (i < (argc - 1))
	// {
	// 	result[i] = ft_atoi(nums_arr[argc - 1 - 1 - i]);
	// 	printf("%d, ", result[i]);
	// 	i++;
	// }
	return (0);
}